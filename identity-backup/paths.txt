# Paths are relative to your home directory.
# Blank lines and lines beginning with # are ignored.

# SSH keys and configuration
.ssh

# GPG private keys, public keys, and trust database
.gnupg

# SOPS and age keys
.config/sops
.config/age
.age

# Docker login, contexts, and configuration
.docker

# Kubernetes configs, including .kube/*.kubeconfig.yaml
.kube

# Other common identity and credential files
.gitconfig
.config/git
.password-store
.netrc
.authinfo
.config/gh
.config/glab
.aws
.azure

# Do not restore ~/.local/share/keyrings directly on a new system. The GNOME
# login keyring is encrypted with the old login password and will break automatic
# unlock after a password or machine migration. Export/import individual secrets
# intentionally instead.
