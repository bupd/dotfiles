# Repository Instructions

- Use `git commit -sm "<message>"` for commits in this repository and its submodules unless the user explicitly asks for a different commit form.
- Use Brave for Playwright/browser automation. Chrome does not have the required Playwright extension installed.
